> 一旦我所属的文件夹有所变化，请更新我。

# 日志脱敏模块

该模块提供日志输出前的脱敏与截断能力，防止敏感信息（密码、Token、密钥等）泄露到日志文件中，同时限制超长对象的日志体积。纯核心实现，无 Spring 依赖，可在任意层级直接调用。

| 文件 | 地位 | 功能 |
|------|------|------|
| `LogSanitizer.java` | Core | 对象 JSON 序列化脱敏 + 长度截断 |
