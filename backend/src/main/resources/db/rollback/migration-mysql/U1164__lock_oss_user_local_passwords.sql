-- Input: V1164__lock_oss_user_local_passwords.sql
-- 回滚 V1164：无法安全回滚
-- V1164 将 OSS 用户的本地密码统一置为锁定哈希。回滚需要恢复每个用户的原始本地密码，
-- 但生产环境并不存储 OSS 用户密码，原始值不可恢复；若统一恢复为旧的默认测试密码 123456，
-- 会重新引入高危安全漏洞。因此本回滚脚本为 No-op rollback（空操作）。
-- 如业务上确实需要为个别 OSS 用户开启本地登录，应由管理员单独生成强密码并更新该用户记录。
