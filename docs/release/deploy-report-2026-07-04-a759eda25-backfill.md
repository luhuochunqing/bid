# a759eda25 生产部署报告（补录）

> ⚠️ **补录说明**：本次部署于 2026-07-04 19:59 CST 执行，但当时未生成部署报告，违反"每次部署必须生成报告"纪律（skill 第 7 条经验）。本报告于 2026-07-05 补录，基于 git log 和服务器 deployed-release.json 历史记录还原。

## 部署概览

| 项目 | 值 |
|---|---|
| 部署日期 | 2026-07-04 19:59 CST（估算，基于 `/opt/xiyu-bid/db-backups/winbid-a759eda25-20260704195944.sql.gz` 时间戳） |
| Release ID | `a759eda25-api8080` |
| 上一版本 | `eb8ce51a5-api8080`（第 45 次，2026-07-04 18:24 CST） |
| 下一版本 | `c9f36c23d-api8080`（第 46 次，2026-07-04 22:58 CST） |
| 部署类型 | 增量部署（业务功能更新 + UI 优化，无 DB 迁移） |
| 部署结果 | ✅ 成功（后续第 46 次部署时服务器健康检查正常） |

## 基线信息

| 项目 | 值 |
|---|---|
| HEAD commit | `a759eda25`（推测，基于 deployed-release.json 记录） |
| origin/main 同步 | ✅ 已同步（第 46 次部署的早操确认 HEAD = origin/main） |

## 增量 PR 列表（10 个 commit，`eb8ce51a5..a759eda25`）

| Commit | PR | 描述 |
|---|---|---|
| `e9134b64c` | — | docs(release): 第 45 次生产部署报告 |
| `39947fde1` | — | fix(personnel): CO-469 第七轮，ExportProgress.recordCount → totalCount 字段名对齐前端 |
| `dc0feab43` | !1682 | docs(release): 第 45 次生产部署报告 |
| `ab332bc3c` | !1684 | fix(personnel): CO-469 第七轮，ExportProgress.recordCount → totalCount 字段名对齐前端 |
| `07e1879d7` | — | docs(lessons): CO-498 根因分析 + §38 修 bug 时删除代码必须审视隐式字段契约 |
| `0f8701b98` | !1685 | docs(lessons): CO-498 根因分析 + §38 修 bug 时删除代码必须审视隐式字段契约 |
| `f9acb43e6` | — | feat(casework): CO-496 项目档案新增文档分类下载文件视图 |
| `c2e8a732c` | — | CO-484 v2 优化: 审核人多人显示 + 审核记录展示 |
| `e6fc6dade` | !1683 | feat(casework): CO-496 项目档案新增文档分类下载文件视图 |
| `a759eda25` | !1686 | CO-484 v2 优化: 审核人多人显示 + 审核记录展示 |

## 改动范围

**核心业务变更**（3 个功能模块）：

### 1. CO-484 v2 优化：审核人多人显示 + 审核记录展示
- 审核人界面支持多人同时显示
- 新增审核记录展示功能
- 涉及模块：标书审核流程 UI

### 2. CO-496：项目档案新增文档分类下载文件视图（feat(casework)）
- 项目档案模块新增按文档分类下载的文件视图
- 涉及模块：项目档案/文档管理

### 3. CO-469 第七轮：人员证书导出字段名修复（fix(personnel)）
- `ExportProgress.recordCount` → `totalCount` 字段名对齐前端
- 涉及模块：人员证书导出功能

### 4. 文档沉淀
- CO-498 根因分析 + §38 修 bug 时删除代码必须审视隐式字段契约
- 第 45 次生产部署报告

## Flyway 预检

| 步骤 | 结果 | 备注 |
|---|---|---|
| 迁移文件变更 | 无 | `eb8ce51a5..a759eda25` 无迁移文件变更 |
| DB 版本 | 一致 | DB V1134 = 源码 V1134（无新迁移） |

**结论**：无新迁移需要应用，纯业务代码 + UI 优化部署。

## 验证结果

> 注：本次部署未留存独立的 Smoke 测试记录。后续第 46 次部署（2 小时 59 分后）的 Flyway validate 和健康检查均正常，且 `a759eda25` 作为上一版本时服务运行稳定，可间接验证本次部署成功。

| 间接证据 | 来源 |
|---|---|
| 第 46 次部署时 Flyway validate 通过 | 第 46 次报告 Step 3 |
| 第 46 次部署时服务器健康检查正常 | 第 46 次报告，服务器 `deployed-release.json` 指向 `a759eda25` |
| 后端服务运行 2 小时 59 分无异常 | 第 46 次部署前查询（22:56 CST） |

## 回滚信息

| 项目 | 值 |
|---|---|
| 上一可用 release | `eb8ce51a5-api8080`（第 45 次，2026-07-04 18:24 CST） |
| DB 备份 | `/opt/xiyu-bid/db-backups/winbid-a759eda25-20260704195944.sql.gz`（3.2M，部署前生成） |
| 回滚需求 | 不需要（已成功运行并被第 46 次部署替换） |

## 经验沉淀应用情况

| 经验 | 本次应用 |
|---|---|
| Flyway 预检 3 步法 | ✅ 无新迁移，无需预检 |
| 同源构建 baseURL="" | ✅ 构建方式与第 45/46 次一致 |
| systemctl sudo | ✅ `SYSTEMCTL_SUDO=true` 默认启用 |

## 补录确认清单

- [x] 增量 commit 范围确认（`eb8ce51a5..a759eda25`，共 10 个 commit）
- [x] 迁移文件变更确认（无变更）
- [x] 数据库备份确认（`winbid-a759eda25-20260704195944.sql.gz` 存在）
- [x] 间接验证：第 46 次部署时服务运行正常
- [x] 回滚信息记录

---

**部署执行人**：Trae Agent（主工作区，推测）
**补录时间**：2026-07-05 08:05 CST
**补录原因**：原始部署时未生成报告，为保持部署历史完整性补录